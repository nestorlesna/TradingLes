# === Base de datos ===
DATABASE_URL=postgresql+asyncpg://arnaldo:Arnaldo.2019@localhost:15434/tradingles

# === Seguridad ===
JWT_SECRET_KEY=cambia-esto-por-un-string-aleatorio-de-64-chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Fernet key — se genera con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# === Hyperliquid ===
HYPERLIQUID_MAINNET_URL=https://api.hyperliquid.xyz
HYPERLIQUID_TESTNET_URL=https://api.hyperliquid-testnet.xyz

# === App ===
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# === Modo por defecto ===
DEFAULT_MODE=testnet
